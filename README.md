1.Open VS code and copy the code
In terminal
2.git init
3.git remote add origin https://github.com/rmshriya/ai_heatmap.git
4.git add .
5.git commit -m "Initial commit"
6.git pull origin main --allow-unrelated-histories
7.git push -u origin main
8.python train_model.py
9.streamlit run app.py
Streamlit page will open and AI-ML Heatma Model is deployed
